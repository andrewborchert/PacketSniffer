# PacketSniffer
Basic packet sniffer built with Python and Scrappy. Listens to live network traffic and prints a summary of each packet captured.

- Captures live packets on your network
- Prints a summary for each packet
- Lightweight and easy to understand

### Prerequisites
- Python 3.x
- Scapy library

Install dependencies:
```bash
pip install -r requirements.txt
